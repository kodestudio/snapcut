class MonHoc {
  final int id;
  final String tenMon;
  final String icon;
  final bool isChecked;

  double get diemTB {
    // Công thức tính
    throw UnimplementedError();
  }

  const MonHoc({
    required this.id,
    required this.tenMon,
    required this.icon,
    required this.isChecked,
  });
}
