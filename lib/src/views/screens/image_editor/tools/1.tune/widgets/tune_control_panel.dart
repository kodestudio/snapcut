import 'package:flutter/material.dart';

class TuneControlPanel extends StatelessWidget {
  const TuneControlPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
