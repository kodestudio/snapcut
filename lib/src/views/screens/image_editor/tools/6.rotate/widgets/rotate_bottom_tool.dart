import 'package:flutter/material.dart';

class RotateBottomTool extends StatelessWidget {
  const RotateBottomTool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
