import 'package:flutter/material.dart';

class RotateTopTool extends StatelessWidget {
  const RotateTopTool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Column(
        children: [
          AppBar(),
        ],
      ),
    );
  }
}
