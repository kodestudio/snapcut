import 'package:flutter/material.dart';

class SideTool extends StatelessWidget {
  const SideTool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
